# 多标签页协调

## 目标

避免同一浏览器内多个标签页同时承担世界主实例职责，导致重复抓源、重复写状态或互相覆盖。

## 核心机制

- 主实例选举
- 心跳与失活检测
- 只允许主实例执行高成本后台任务

## 主实例职责

- 主导消息源拉取
- 主导后台自动互动
- 主导世界状态写入

## 非主实例职责

- 只读状态或轻量交互
- 必要时向主实例派发命令

## 切换原则

- 主实例失活时再选举
- 页面不可见不必立即失去主实例资格，但需要结合运行时策略降级

## 边界

多标签页协调属于运行时问题，不属于世界业务问题。
